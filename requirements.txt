# Core
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.26.0
tokenizers>=0.19.0

# Quantization (optional, for running on low VRAM)
bitsandbytes>=0.41.0

# Fine-tuning (optional)
peft>=0.7.0
trl>=0.7.0
datasets>=2.14.0

# API server (optional)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
